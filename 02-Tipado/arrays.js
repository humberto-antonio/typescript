"use strict";
const numeros = [1, "2"];
numeros.push(2);
const arreglos = ['Hola', 1];
//const constante: (string | number) = 3
const numeros2 = [1, 2, 3, 4, 5];
numeros2.forEach(n => console.log(n.toFixed(1)));
const nombres = ['Humberto', 'Antonio', 'Juan'];
nombres.forEach(nombre => console.log(nombre.toUpperCase()));
